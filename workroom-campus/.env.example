# Never commit the real key. Add the real value in Vercel Project Settings → Environment Variables.
GEMINI_API_KEY=replace_with_a_newly_created_key
# Optional: change this if your Google AI Studio account uses another supported Gemini model.
GEMINI_MODEL=gemini-3.7-flash
